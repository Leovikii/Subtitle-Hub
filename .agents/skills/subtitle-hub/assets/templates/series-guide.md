# {{SERIES_TITLE}} 系列用语规范

本文件是本系列唯一活动术语表，遵循 `$subtitle-hub` 的 `SH-TRANS-007`。项目不得复制本表；差异只能在项目 `docs/project-guide.md` 通过 `term_id` 明确覆盖。

## 来源

| source_id | 类型 | 标题 | URL 或仓库路径 | 核验日期 | 用途与限制 |
| --- | --- | --- | --- | --- | --- |

## 规范术语

| term_id | 类别 | 日文原词 | 规范简体中文 | 禁用或待替换形式 | 适用范围 | source_id | 状态 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |

状态只使用 `confirmed`、`provisional` 或 `pending-user-confirmation`。歧义项确认前不得批量替换。

## 待用户确认

无。新增歧义时列出候选译名、原词、证据、影响项目和待确认问题。
