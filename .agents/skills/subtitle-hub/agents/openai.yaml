interface:
  display_name: "Subtitle Hub"
  short_description: "探测素材、初始化、校对并发布 Subtitle Hub 字幕"
  default_prompt: "Use $subtitle-hub to probe my video and Chinese subtitle baseline, resolve the required material mapping and project name, then initialize or continue the project safely."

policy:
  allow_implicit_invocation: true
