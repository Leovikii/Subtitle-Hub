# Subtitle Hub Agent Instructions

本仓库所有建项、素材清点、字幕校对、构建、验证和发布工作必须使用仓库级 `$subtitle-hub` Skill：`.agents/skills/subtitle-hub/SKILL.md`。Skill 是仓库级规范唯一事实源；不得另建并行规范，也不得从历史台账引用还原旧标准。

按 Skill 路由读取当前任务所需 reference，再依次读取系列 `series-guide.md`（若存在）、作品 `project.yaml` 和项目根目录 `review.md`。

新建项目从中文字幕底稿和可用文本证据开始；用户提供视频时才主动探测音轨与字幕轨。一次确认中批准关联作品/范围、素材映射、时间轴权威、发布语言和简短项目名。批准后 dry-run 作为内部安全检查；结果无实质变化时不得再打断用户。建项直接生成全局 Noto 字体母本，并以 `--ready-for-proofreading` 为内容门禁。

规则优先级为：用户当次明确要求、`project.yaml` 中已确认的项目覆盖项、系列确认术语、Skill、Skill 指定的单一外部样式基准。偏离系列译名必须在 `project.yaml` 记录用户确认。项目当前状态、本轮方案、逐项决定、实际修改和验证只以根目录 `review.md` 为准，关闭历史由 Git 保存。

## 不可违反的工作边界

- `project/sources/` 是只读来源，不得原地修改。
- 不得直接编辑 `subtitles/current/`；修订、构建和发布轮换必须按 Skill 在 `project/workspace/` 中完成。
- 自动检测结果只是候选问题，未经证据确认不得批量改写；不得虚构人工复核或全片观看覆盖。
- 发布范围内全部可见中文字幕必须完成检查；素材不足只能降低证据等级，不能把抽查写成全文完成。文本优先，视频只用于明确的时间轴、特殊字幕、局部布局或画面语境疑点；不得引入 ASR、VAD、OCR 生成或全片媒体处理。
- 实质字幕修改必须先在 `review.md` 提交详细分类校对方案；重译、删减、增译和对白改正逐条列出，只有同规则同类别的机械修改可合并。同一条目持续更新用户决定、实际结果与验证，不另建台账或轮次报告。方案批准后连续完成实现、验证和发行候选，不为常规检查另设中断；用户通过发行前终审后方可发布。
- 目录只在写入首个对应文件时创建。`temp/` 只允许按需的 `tools/` 与 `notes/`；不得创建 `archive/`、截图证据树、`intermediate/` 或 `logs/`。
- 项目控制文件只有 `project.yaml` 与根目录 `review.md`。不得新增审计/coverage sidecar、逐集报告、项目脚本副本或占位文件；intake、episode map 和逐事件审计明细放系统临时目录并在结论写回后清理。
- 视觉核对必须在本地按候选时间点完成，不得在聊天或对话中批量附加视频截图、连续帧、联系表或其他大量图像证据，以免触发 Cloudflare CDN `413 Payload Too Large`。默认只在报告中记录时间点、文字结论和本地证据路径；只有用户明确要求查看单点证据时，才可在单条消息中附加不超过 2 张经过压缩的必要截图。
