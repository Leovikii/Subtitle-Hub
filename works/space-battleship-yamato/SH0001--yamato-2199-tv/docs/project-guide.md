# SH0001《宇宙战舰大和号2199》TV 项目补充规范

本文件是 SH0001 唯一可以补充或覆盖系列及仓库全局规范的文档。未列出的系列译名遵循上级 `series-guide.md`，其他事项遵循仓库全局规范；偏离系列译名必须记录用户确认。当前状态与校对报告以 `review.md` 为准，逐项候选、决定、修改和验证以 `ledger.tsv` 为准。

## 1. 范围与语言

- 作品：TV 正篇 26 集；
- 中文主字幕：简体中文；
- 副字幕：日语；
- 目标片源：`sav1our-bd-1080p-av1`；
- 不包含：OVA、《星巡的方舟》及其他剧场版。

## 2. 来源权威分工

### 时间轴和分段

目标 `[Sav1our]` 视频内嵌英文 ASS 是时间边界、分段、画面文字位置和目标文件命名的主参考。英文文本本身不是日语原文或中文翻译权威。

### 原语文本

`ja-official-webrip-cc` 日语 WEBrip CC 是普通对白日文正文主参考，但需映射到 `[Sav1our]` 时间轴，并排除听障说话人标签、纯音效括注和语言提示。CC 与实际音轨明显冲突时必须回听。

### 中文底稿和备用来源

`zh-Hans-ja-kamigami-bilingual-fallback` 用作中文底稿、旧画面排版和检索锚点；`ja-kamigami-fallback` 只在官方日文无法可靠对应时辅助定位。诸神旧日文不得反向覆盖官方日语或实际音轨。

## 3. 本项目重点风险

- 舰名、人物名、军衔、装备、组织和系列术语不一致；
- 否定、数量、方位、军事命令和因果关系错译；
- 旧日文听写造成的连带错译；
- 英语中转造成的语义偏移；
- 为求华丽添加原文外信息，或加入吐槽、出处和无关科普；
- 无法可靠映射的日语被相邻台词强行补齐；
- 顶部通讯、外星语和真实画面文字被统一重置到底部。

系列共有译名必须遵循上级 [series-guide.md](../../series-guide.md)，不得在本项目重复建立术语表。发现既有成品与系列表不一致时先登记候选并逐事件核对，不得直接批量替换；证据不足时使用中性中文并登记问题。

## 4. 样式与特殊布局

| 用途 | 中文样式 | 日语样式 | 说明 |
| --- | --- | --- | --- |
| 普通对白 | `CN-Main` | `JP-Main` | 中文上、日文下，同起止时间 |
| 顶部通讯/广播 | `CN-Top` | `JP-Top` | 避让底部原生文字或外星语画面字幕 |
| 外星语翻译 | `CN-Alien` | 按画面需要 | 不遮挡原生日语字幕 |
| ED 歌词 | `ED-CN` | `ED-JP` | 中日逐条同进同退 |
| 科学短注 | `CN-Science-Note` | 无 | 左上安全区单行短注 |

1.0.0 保留既有视觉历史；下一内容版本的普通中文对白按 `SH-LAYOUT-005` 统一，日文副语仍保持较低视觉权重。特殊布局不随正文批量重置。

## 5. 项目补充和覆盖规则

| rule_id | type | global_ref | scope | rule | rationale |
| --- | --- | --- | --- | --- | --- |
| `SH0001-RULE-001` | `supplement` | `SH-TIME-004` | 全 26 集 | `[Sav1our]` 内嵌时间轴优先保留；普通数值偏差只生成候选，触发纠错底线时必须回到音画局部修正并登记依据。 | 内嵌轨与目标视频同源，但不能为严重错误免责。 |
| `SH0001-RULE-002` | `override` | `SH-ZH-002` | 普通中文对白 | 不使用逗号、句号、顿号和分号；普通停顿使用一个半角空格。 | 延续本项目 1.0.0 已验证的极简标点体系。 |
| `SH0001-RULE-003` | `supplement` | `SH-LAYOUT-002` | 普通对白、顶部通讯、ED | 已配对中日事件必须共享完全相同的起止时间。 | 防止副语漂移和错误映射。 |
| `SH0001-RULE-004` | `supplement` | `SH-QC-007` | ED | 保留中日 ED 歌词。 | 属于作品存档需求，不套用平台歌曲交付限制。 |
| `SH0001-RULE-005` | `supplement` | `SH-ZH-005` | 科学短注 | 只保留已重新制作且与理解剧情直接相关的 14 条短注。 | 避免恢复吐槽、出处和长篇科普。 |
| `SH0001-RULE-006` | `supplement` | `SH-REL-004` | 署名元数据 | 原稿中可识别的字幕组和完整制作人员分工只允许合并保存为 `[Script Info]` 中唯一一条 `Subtitle-Hub-Source-Credit`；不得写入 Events。 | 正式画面不得显示制作署名，同时保留完整原始制作信用。 |
| `SH0001-RULE-007` | `supplement` | `SH-LAYOUT-003` | 特殊布局 | 顶部通讯、外星语和真实画面文字必须逐场景复核，不得统一重置为底部。 | 需要避让原生画面文字和动作。 |

## 6. 工程区约束

- 只编辑 `project/workspace/episodes/S01E01/master.ass` 至 `S01E26/master.ass`；
- 项目专用脚本按需放入 `project/workspace/temp/tools/`，补充引导按需放入 `temp/notes/`；
- 普通中间输出使用系统临时目录；完整发行候选按需生成到 `project/workspace/build/`，不预建空目录；
- 逐项候选、用户决定、实施和验证写入 `ledger.tsv`，当前轮次和摘要写入 `review.md`；

## 7. 已知限制和禁止自动处理项

- 1.0.0 是可继续校订的迁入基线，不代表语言层面永久定稿；
- 历史脚本包含旧目录硬编码和剧场版假设，禁止在当前工程直接运行；
- 无法可靠映射的日语、顶部通讯、外星语、歌曲和特殊画面文字必须人工判断；
- 不得批量覆盖主稿，不得机械修改时间轴、分轴、样式或特效位置。
